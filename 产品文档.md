
碎片框/轻闪框/中转框 - Mac超轻量菜单栏Markdown编辑区


产品需求文档：
1. 产品概述
1.1 产品名称
中文名称：轻闪框
英文名称：Flash box
1.2 产品定位
一款基于macOS原生开发、极致轻量化的Markdown文本编辑器，完全复刻MarkEdit全部核心编辑能力，额外新增菜单栏常驻临时文本中转编辑面板。主打无打扰、秒开启、极速记录，兼顾正式Markdown文档编辑与碎片化临时文字中转存储，专为Mac高效办公、快速记录人群设计，全程本地存储、零后台联网、极低内存占用。

1.3 设计理念
- 极致轻量：安装包体积≤5MB，后台常驻内存占用≤30MB，无冗余插件、无多余弹窗
- 极简主义：
- 原生适配：严格遵循macOS系统设计规范，适配深色/浅色模式、系统快捷键、触控板操作
- 双模式兼顾：既保留MarkEdit专业Markdown编辑能力，又新增菜单栏临时速记面板，满足临时中转+正式文档双重需求
- 隐私优先：默认离线运行，无数据收集、无自动云同步，所有文本本地加密存储，同步功能手动自主开启

1.4 产品背景
目前主流Mac轻量Markdown编辑器（如MarkEdit）专注于正式文档编辑，缺少碎片化临时记录能力。用户日常使用中，常需要临时复制中转文字、快速记录灵感、留存临时代码片段、摘抄短句，打开完整编辑器流程繁琐、占用窗口空间。本产品在保留MarkEdit简洁纯净、原生流畅优势的基础上，增加菜单栏一键唤起临时编辑框，填补轻量化Markdown编辑器的临时文字中转空白，实现「临时速记+正式编辑」一体化。


2. 用户需求分析
2.1 目标用户
1. 办公上班族：需要临时留存会议短句、复制中转文案、整理工作碎片信息
2. 技术开发者：临时存放代码片段、接口文本、调试备注，同时需要轻量化Markdown编辑文档
3. 文案创作者：随时记录灵感、摘抄素材，后期整理成正式Markdown文档
4. 极简主义Mac用户：厌恶臃肿软件，追求低占用、无广告、纯净原生的文本工具
2.2 用户痛点
- 普通备忘录、记事本层级太深，快速记录操作繁琐，无法一键唤起
- 现有Markdown编辑器无临时中转面板，复制临时文本需新建文件，流程冗余
- 多数编辑器体积臃肿、后台占用内存高，影响Mac设备流畅度
- 部分工具强制联网、收集用户文本数据，隐私安全性无法保障
- 临时记录文本无Markdown语法支持，后期排版整理成本高
2.3 用户核心诉求
- 常驻菜单栏，单击图标秒开临时编辑框，无需新建文件、无多余跳转
- 软件极致轻量，不占用系统资源，开机静默后台常驻
- 操作简洁无学习成本，适配Mac原生操作逻辑



3. 产品功能架构
3.1 功能总览
产品分为菜单栏临时速记模块（新增独创功能）和主程序Markdown编辑模块（复刻MarkEdit完整版功能）两大板块，双模块数据互通，互不干扰，轻量化并行运行。


3.2 核心功能：菜单栏临时速记面板（差异化新增功能）应用常驻Mac顶部菜单栏，显示极简自定义图标，单击图标直接在图标下方弹出悬浮空编辑框，无需打开独立窗口，用于临时文字中转、快速灵感记录、临时代码留存；
3.2.2 详细功能细则
1. 菜单栏常驻图标
  - 可在设置中设置开机自启，后台静默运行，内存占用极低；
  - 尺寸形态：默认窄长矩形适配屏幕顶部，支持手动拖拽调整宽高，置顶不被遮挡；
2.单击图标即在下方出现编辑框，浮到最上层；（这个可以实现吗？）
  - 再次单击时，编辑框消失隐藏；
  - 编辑框可拖拽大小和位置，就像拖拽mac自带的便签一样；
3，右键图标出现：
  - 1，新建编辑框；（注意右键点击图标时，也有新建一个编辑框。注意，点击常驻菜单栏时，所有的编辑框是一起隐藏的）（理想状态用户不需要建多个编辑框，但如果用户建了多个编辑框，那么这个菜单后的编辑框就会增加（最多5个），那么当用户不想要这么多编辑框了怎么办？就在菜单的里面对应的编辑框删除。这里，如果全部都删除了，那么下次点击图标时，会自动新建一个编辑框）
  - 2，快捷键显示，全局快捷键：自定义快捷键（默认fn+Space）一键唤起/隐藏临时编辑框；这个快捷键的效果，其实就是左键单击图标，按快捷键及唤醒，按快捷键及收起；
  - 3，编辑框1，后有删除按钮
  - 4，编辑框2，后有删除按钮
  - 5，编辑框n，后有删除按钮
  - 6，颜色切换
  - 7，退出

4.编辑框上有操作按钮：
  - 最小按钮：点击后及收起编辑框；  
  - 格式操作：点击格式操作后，有列表、有序列表、待办；当把下面文字改为代办时，要渲染出勾选选中和未选中的效果；
  - 清空操作：点击后一键清空当前编辑框内容；
  - 更多操作：点击更多操作后有保存为文档、删除选项
  - cmd+n：新建一个编辑框；（这个和右键点击图标那个新建功能一致）
  - cmd+s：是调动出保存，保存后和markedit的保存一样，将当前的编辑框保存到后续指定的位置；注意，当保存以后相当于这个md保存并关闭了，然后在原位置出现了同尺寸的一个新的编辑框；

5，其他：
  - 自动保存：输入内容实时本地缓存，关闭弹窗不丢失数据，再次打开延续上次内容；（除非保存为md文档，或删除，否则内容就存在盒子里）







